// Side nav and main content widths in units of vw
const sideNavWidth = 18;
const mainWidth = 100-sideNavWidth;

// The main content header, footer, and body heights in units of vh
const footerHeight = 5;
const headerHeight = 10;
const bodyHeight = 100-footerHeight-headerHeight;

module.exports = {
  sideNavWidth,
  mainWidth,
  footerHeight,
  headerHeight,
  bodyHeight,
}

const tablePositioning = ''
const tableStyling = ''

const DataTable = () => {
  return (
    <div className={`${tablePositioning} ${tableStyling}`}>
      <h1>DataTable</h1>
    </div>
  )
};

export default DataTable;
